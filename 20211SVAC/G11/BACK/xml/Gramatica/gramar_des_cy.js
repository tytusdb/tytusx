/* parser generated by jison 0.4.18 */
/*
  Returns a Parser object of the following structure:

  Parser: {
    yy: {}
  }

  Parser.prototype: {
    yy: {},
    trace: function(),
    symbols_: {associative list: name ==> number},
    terminals_: {associative list: number ==> name},
    productions_: [...],
    performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate, $$, _$),
    table: [...],
    defaultActions: {...},
    parseError: function(str, hash),
    parse: function(input),

    lexer: {
        EOF: 1,
        parseError: function(str, hash),
        setInput: function(input),
        input: function(),
        unput: function(str),
        more: function(),
        less: function(n),
        pastInput: function(),
        upcomingInput: function(),
        showPosition: function(),
        test_match: function(regex_match_array, rule_index),
        next: function(),
        lex: function(),
        begin: function(condition),
        popState: function(),
        _currentRules: function(),
        topState: function(),
        pushState: function(condition),

        options: {
            ranges: boolean           (optional: true ==> token location info will include a .range[] member)
            flex: boolean             (optional: true ==> flex-like lexing behaviour where the rules are tested exhaustively to find the longest match)
            backtrack_lexer: boolean  (optional: true ==> lexer regexes are tested in order and for each matching regex the action code is invoked; the lexer terminates the scan when a token is returned by the action code)
        },

        performAction: function(yy, yy_, $avoiding_name_collisions, YY_START),
        rules: [...],
        conditions: {associative list: name ==> set},
    }
  }


  token location info (@$, _$, etc.): {
    first_line: n,
    last_line: n,
    first_column: n,
    last_column: n,
    range: [start_number, end_number]       (where the numbers are indexes into the input string, regular zero-based)
  }


  the parseError function receives a 'hash' object with these members for lexer and parser errors: {
    text:        (matched text)
    token:       (the produced terminal token, if any)
    line:        (yylineno)
  }
  while parser (grammar) errors will also provide these members, i.e. parser errors deliver a superset of attributes: {
    loc:         (yylloc)
    expected:    (string describing the set of expected tokens)
    recoverable: (boolean: TRUE when the parser has a error recovery rule available for this particular error)
  }
*/
var gramar_des_cy = (function(){
var o=function(k,v,o,l){for(o=o||{},l=k.length;l--;o[k[l]]=v);return o},$V0=[2,11],$V1=[1,7],$V2=[1,8],$V3=[2,6],$V4=[2,6,11],$V5=[13,17],$V6=[1,15],$V7=[2,18],$V8=[1,27],$V9=[1,28],$Va=[1,29],$Vb=[1,30],$Vc=[1,31],$Vd=[1,32],$Ve=[2,6,11,15],$Vf=[2,31],$Vg=[2,22],$Vh=[15,27,28,29,30,31,32];
var parser = {trace: function trace () { },
yy: {},
symbols_: {"error":2,"start":3,"encod":4,"raices":5,"EOF":6,"XML":7,"raiz":8,"raiz_p":9,"elemento":10,"ETI_A":11,"list_atributos":12,"ETI_CS":13,"elementos":14,"ETI_C":15,"list_elementos":16,"ETI_CC":17,"atributos":18,"list_text_elemento":19,"atributo":20,"atributo_p":21,"ID":22,"IGUAL":23,"CADENA":24,"tipo":25,"lte_p":26,"TEXTO":27,"LESS":28,"MORE":29,"AMP":30,"APOS":31,"QUOT":32,"elementos_p":33,"$accept":0,"$end":1},
terminals_: {2:"error",6:"EOF",7:"XML",11:"ETI_A",13:"ETI_CS",15:"ETI_C",17:"ETI_CC",22:"ID",23:"IGUAL",24:"CADENA",27:"TEXTO",28:"LESS",29:"MORE",30:"AMP",31:"APOS",32:"QUOT"},
productions_: [0,[3,3],[4,1],[4,0],[5,2],[9,2],[9,0],[8,1],[8,1],[10,6],[10,6],[10,3],[12,1],[12,0],[16,1],[16,0],[18,2],[21,2],[21,0],[20,3],[19,2],[26,2],[26,0],[25,1],[25,1],[25,1],[25,1],[25,1],[25,1],[14,2],[33,2],[33,0]],
performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate /* action[1] */, $$ /* vstack */, _$ /* lstack */) {
/* this == yyval */

var $0 = $$.length - 1;
switch (yystate) {
case 1:
 this.$ = new Node_tree('start',''); this.$.addChild($$[$0-2]); this.$.addChild($$[$0-1]); return this.$; 
break;
case 2:
 this.$ = new Node_tree('encod',''); this.$.addChild(new Node_tree($$[$0],'XML')); 
break;
case 3:
 this.$ = new Node_tree('encod',''); this.$.addChild(new Node_tree('ε','ε')); 
break;
case 4:
 this.$ = new Node_tree('raices',''); this.$.addChild($$[$0-1]); this.$.addChild($$[$0]); 
break;
case 5:
 this.$ = new Node_tree('raiz_p',''); this.$.addChild($$[$0-1]); this.$.addChild($$[$0]); 
break;
case 6: case 18:
 this.$ = new Node_tree('raiz_p',''); this.$.addChild(new Node_tree('ε','ε')); 
break;
case 7:
 this.$ = new Node_tree('raiz',''); this.$.addChild($$[$0]); 
break;
case 8:
 this.$ = new Node_tree('error','error'); 
break;
case 9: case 10:
 this.$ = new Node_tree('elemento',''); this.$.addChild(new Node_tree('<'+$$[$0-5],'ETI_A'));  this.$.addChild($$[$0-4]); this.$.addChild(new Node_tree($$[$0-3],'ETI_CS'));  this.$.addChild($$[$0-2]); this.$.addChild(new Node_tree('</'+$$[$0-1],'ETI_C')); this.$.addChild(new Node_tree($$[$0],'ETI_CS')); 
break;
case 11:
 this.$ = new Node_tree('elemento',''); this.$.addChild(new Node_tree('<'+$$[$0-2],'ETI_A'));  this.$.addChild($$[$0-1]); this.$.addChild(new Node_tree($$[$0],'ETI_CC'));  
break;
case 12:
 this.$ = new Node_tree('list_atributos',''); this.$.addChild($$[$0]); 
break;
case 13:
 this.$ = new Node_tree('list_atributos',''); this.$.addChild(new Node_tree('ε','ε')); 
break;
case 14:
 this.$ = new Node_tree('list_elementos',''); this.$.addChild($$[$0]); 
break;
case 15:
 this.$ = new Node_tree('list_elementos',''); this.$.addChild(new Node_tree('ε','ε')); 
break;
case 16:
 this.$ = new Node_tree('atributos',''); this.$.addChild($$[$0-1]); this.$.addChild($$[$0]); 
break;
case 17:
 this.$ = new Node_tree('atributo_p',''); this.$.addChild($$[$0-1]); this.$.addChild($$[$0]); 
break;
case 19:
 this.$ = new Node_tree('atributo',''); this.$.addChild(new Node_tree($$[$0-2],'ID')); this.$.addChild(new Node_tree($$[$0-1],'IGUAL')); this.$.addChild(new Node_tree($$[$0],'CADENA')); 
break;
case 20:
 this.$ = new Node_tree('list_text_elemento',''); this.$.addChild($$[$0-1]); this.$.addChild($$[$0]); 
break;
case 21:
 this.$ = new Node_tree('lte_p',''); this.$.addChild($$[$0-1]); this.$.addChild($$[$0]); 
break;
case 22:
 this.$ = new Node_tree('lte_p',''); this.$.addChild(new Node_tree('ε','ε')); 
break;
case 23:
 this.$ = new Node_tree('tipo',''); this.$.addChild(new Node_tree($$[$0],'TEXTO'));   
break;
case 24:
 this.$ = new Node_tree('tipo',''); this.$.addChild(new Node_tree($$[$0],'LESS'));   
break;
case 25:
 this.$ = new Node_tree('tipo',''); this.$.addChild(new Node_tree($$[$0],'MORE'));  
break;
case 26:
 this.$ = new Node_tree('tipo',''); this.$.addChild(new Node_tree($$[$0],'AMP'));  
break;
case 27:
 this.$ = new Node_tree('tipo',''); this.$.addChild(new Node_tree($$[$0],'APOS'));  
break;
case 28:
 this.$ = new Node_tree('tipo',''); this.$.addChild(new Node_tree($$[$0],'QUOT'));  
break;
case 29:
 this.$ = new Node_tree('elementos',''); this.$.addChild($$[$0-1]); this.$.addChild($$[$0]); 
break;
case 30:
 this.$ = new Node_tree('elementos_p',''); this.$.addChild($$[$0-1]); this.$.addChild($$[$0]); 
break;
case 31:
 this.$ = new Node_tree('elementos_p',''); this.$.addChild(new Node_tree('ε','ε')); 
break;
}
},
table: [o($V0,[2,3],{3:1,4:2,7:[1,3]}),{1:[3]},{2:$V1,5:4,8:5,10:6,11:$V2},o($V0,[2,2]),{6:[1,9]},{2:$V1,6:$V3,8:11,9:10,10:6,11:$V2},o($V4,[2,7]),o($V4,[2,8]),o($V5,[2,13],{12:12,18:13,20:14,22:$V6}),{1:[2,1]},{6:[2,4]},{2:$V1,6:$V3,8:11,9:16,10:6,11:$V2},{13:[1,17],17:[1,18]},o($V5,[2,12]),o($V5,$V7,{21:19,20:20,22:$V6}),{23:[1,21]},{6:[2,5]},{10:24,11:$V2,14:22,15:[2,15],16:23,19:25,25:26,27:$V8,28:$V9,29:$Va,30:$Vb,31:$Vc,32:$Vd},o($Ve,$V0),o($V5,[2,16]),o($V5,$V7,{20:20,21:33,22:$V6}),{24:[1,34]},{15:[1,35]},{15:[1,36]},{10:38,11:$V2,15:$Vf,33:37},{15:[2,14]},{15:$Vg,25:40,26:39,27:$V8,28:$V9,29:$Va,30:$Vb,31:$Vc,32:$Vd},o($Vh,[2,23]),o($Vh,[2,24]),o($Vh,[2,25]),o($Vh,[2,26]),o($Vh,[2,27]),o($Vh,[2,28]),o($V5,[2,17]),o([13,17,22],[2,19]),{13:[1,41]},{13:[1,42]},{15:[2,29]},{10:38,11:$V2,15:$Vf,33:43},{15:[2,20]},{15:$Vg,25:40,26:44,27:$V8,28:$V9,29:$Va,30:$Vb,31:$Vc,32:$Vd},o($Ve,[2,9]),o($Ve,[2,10]),{15:[2,30]},{15:[2,21]}],
defaultActions: {9:[2,1],10:[2,4],16:[2,5],25:[2,14],37:[2,29],39:[2,20],43:[2,30],44:[2,21]},
parseError: function parseError (str, hash) {
    if (hash.recoverable) {
        this.trace(str);
    } else {
        var error = new Error(str);
        error.hash = hash;
        throw error;
    }
},
parse: function parse (input) {
    var self = this,
        stack = [0],
        tstack = [], // token stack
        vstack = [null], // semantic value stack
        lstack = [], // location stack
        table = this.table,
        yytext = '',
        yylineno = 0,
        yyleng = 0,
        recovering = 0,
        TERROR = 2,
        EOF = 1;

    var args = lstack.slice.call(arguments, 1);

    //this.reductionCount = this.shiftCount = 0;

    var lexer = Object.create(this.lexer);
    var sharedState = { yy: {} };
    // copy state
    for (var k in this.yy) {
      if (Object.prototype.hasOwnProperty.call(this.yy, k)) {
        sharedState.yy[k] = this.yy[k];
      }
    }

    lexer.setInput(input, sharedState.yy);
    sharedState.yy.lexer = lexer;
    sharedState.yy.parser = this;
    if (typeof lexer.yylloc == 'undefined') {
        lexer.yylloc = {};
    }
    var yyloc = lexer.yylloc;
    lstack.push(yyloc);

    var ranges = lexer.options && lexer.options.ranges;

    if (typeof sharedState.yy.parseError === 'function') {
        this.parseError = sharedState.yy.parseError;
    } else {
        this.parseError = Object.getPrototypeOf(this).parseError;
    }

    function popStack (n) {
        stack.length = stack.length - 2 * n;
        vstack.length = vstack.length - n;
        lstack.length = lstack.length - n;
    }

_token_stack:
    var lex = function () {
        var token;
        token = lexer.lex() || EOF;
        // if token isn't its numeric value, convert
        if (typeof token !== 'number') {
            token = self.symbols_[token] || token;
        }
        return token;
    }

    var symbol, preErrorSymbol, state, action, a, r, yyval = {}, p, len, newState, expected;
    while (true) {
        // retreive state number from top of stack
        state = stack[stack.length - 1];

        // use default actions if available
        if (this.defaultActions[state]) {
            action = this.defaultActions[state];
        } else {
            if (symbol === null || typeof symbol == 'undefined') {
                symbol = lex();
            }
            // read action for current state and first input
            action = table[state] && table[state][symbol];
        }

_handle_error:
        // handle parse error
        if (typeof action === 'undefined' || !action.length || !action[0]) {
            var error_rule_depth;
            var errStr = '';

            // Return the rule stack depth where the nearest error rule can be found.
            // Return FALSE when no error recovery rule was found.
            function locateNearestErrorRecoveryRule(state) {
                var stack_probe = stack.length - 1;
                var depth = 0;

                // try to recover from error
                for(;;) {
                    // check for error recovery rule in this state
                    if ((TERROR.toString()) in table[state]) {
                        return depth;
                    }
                    if (state === 0 || stack_probe < 2) {
                        return false; // No suitable error recovery rule available.
                    }
                    stack_probe -= 2; // popStack(1): [symbol, action]
                    state = stack[stack_probe];
                    ++depth;
                }
            }

            if (!recovering) {
                // first see if there's any chance at hitting an error recovery rule:
                error_rule_depth = locateNearestErrorRecoveryRule(state);

                // Report error
                expected = [];
                for (p in table[state]) {
                    if (this.terminals_[p] && p > TERROR) {
                        expected.push("'"+this.terminals_[p]+"'");
                    }
                }
                if (lexer.showPosition) {
                    errStr = 'Parse error on line '+(yylineno+1)+":\n"+lexer.showPosition()+"\nExpecting "+expected.join(', ') + ", got '" + (this.terminals_[symbol] || symbol)+ "'";
                } else {
                    errStr = 'Parse error on line '+(yylineno+1)+": Unexpected " +
                                  (symbol == EOF ? "end of input" :
                                              ("'"+(this.terminals_[symbol] || symbol)+"'"));
                }
                this.parseError(errStr, {
                    text: lexer.match,
                    token: this.terminals_[symbol] || symbol,
                    line: lexer.yylineno,
                    loc: yyloc,
                    expected: expected,
                    recoverable: (error_rule_depth !== false)
                });
            } else if (preErrorSymbol !== EOF) {
                error_rule_depth = locateNearestErrorRecoveryRule(state);
            }

            // just recovered from another error
            if (recovering == 3) {
                if (symbol === EOF || preErrorSymbol === EOF) {
                    throw new Error(errStr || 'Parsing halted while starting to recover from another error.');
                }

                // discard current lookahead and grab another
                yyleng = lexer.yyleng;
                yytext = lexer.yytext;
                yylineno = lexer.yylineno;
                yyloc = lexer.yylloc;
                symbol = lex();
            }

            // try to recover from error
            if (error_rule_depth === false) {
                throw new Error(errStr || 'Parsing halted. No suitable error recovery rule available.');
            }
            popStack(error_rule_depth);

            preErrorSymbol = (symbol == TERROR ? null : symbol); // save the lookahead token
            symbol = TERROR;         // insert generic error symbol as new lookahead
            state = stack[stack.length-1];
            action = table[state] && table[state][TERROR];
            recovering = 3; // allow 3 real symbols to be shifted before reporting a new error
        }

        // this shouldn't happen, unless resolve defaults are off
        if (action[0] instanceof Array && action.length > 1) {
            throw new Error('Parse Error: multiple actions possible at state: '+state+', token: '+symbol);
        }

        switch (action[0]) {
            case 1: // shift
                //this.shiftCount++;

                stack.push(symbol);
                vstack.push(lexer.yytext);
                lstack.push(lexer.yylloc);
                stack.push(action[1]); // push state
                symbol = null;
                if (!preErrorSymbol) { // normal execution/no error
                    yyleng = lexer.yyleng;
                    yytext = lexer.yytext;
                    yylineno = lexer.yylineno;
                    yyloc = lexer.yylloc;
                    if (recovering > 0) {
                        recovering--;
                    }
                } else {
                    // error just occurred, resume old lookahead f/ before error
                    symbol = preErrorSymbol;
                    preErrorSymbol = null;
                }
                break;

            case 2:
                // reduce
                //this.reductionCount++;

                len = this.productions_[action[1]][1];

                // perform semantic action
                yyval.$ = vstack[vstack.length-len]; // default to $$ = $1
                // default location, uses first token for firsts, last for lasts
                yyval._$ = {
                    first_line: lstack[lstack.length-(len||1)].first_line,
                    last_line: lstack[lstack.length-1].last_line,
                    first_column: lstack[lstack.length-(len||1)].first_column,
                    last_column: lstack[lstack.length-1].last_column
                };
                if (ranges) {
                  yyval._$.range = [lstack[lstack.length-(len||1)].range[0], lstack[lstack.length-1].range[1]];
                }
                r = this.performAction.apply(yyval, [yytext, yyleng, yylineno, sharedState.yy, action[1], vstack, lstack].concat(args));

                if (typeof r !== 'undefined') {
                    return r;
                }

                // pop off stack
                if (len) {
                    stack = stack.slice(0,-1*len*2);
                    vstack = vstack.slice(0, -1*len);
                    lstack = lstack.slice(0, -1*len);
                }

                stack.push(this.productions_[action[1]][0]);    // push nonterminal (reduce)
                vstack.push(yyval.$);
                lstack.push(yyval._$);
                // goto new state = table[STATE][NONTERMINAL]
                newState = table[stack[stack.length-2]][stack[stack.length-1]];
                stack.push(newState);
                break;

            case 3:
                // accept
                return true;
        }

    }

    return true;
}};

     function change_especials(etiqueta){
          if(etiqueta.includes("&lt;")){
               return etiqueta.replace(/&lt;/g,'<');
          }else if(etiqueta.includes("&gt;")){
               return etiqueta.replace(/&gt;/g,'>');
          }else if(etiqueta.includes("&amp;")){
               return etiqueta.replace(/&amp;/g,'&');
          }else if(etiqueta.includes("&apos;")){
               return etiqueta.replace(/&apos;/g,'\'');
          }else if(etiqueta.includes("&quot;")){
               return etiqueta.replace(/&quot;/g,'\"');
          }else{
               return etiqueta;
          }
     }

     function return_only_id(etiqueta){
          if(etiqueta.includes("</")){
               return etiqueta.substr(2,etiqueta.lenght);
          }else{
               return etiqueta.substr(1,etiqueta.lenght);
          }
     }

     function tag_principal(etiqueta){
          if(etiqueta.includes("</")){
               return 'ETI_C'
          }else if(etiqueta.includes("<")){
               return 'ETI_A'
          }else if(etiqueta.includes("/>")){
               return 'ETI_CC'
          }else if(etiqueta.includes(">")){
               return 'ETI_CS'
          }
          return etiqueta;
     }

     function error_semanti(izqid,dereid,fila,columna){
          if(!(izqid==dereid)){
          }
     }

     function get_type_xml(text,fila,columna){
          let termino = "encoding=\"";
          let posicion = text.indexOf(termino);
          let pos_final= posicion+10;
          let posicion2 = text.indexOf('"',pos_final);
          let longitud= posicion2-pos_final;

          if (posicion !== -1){
               return text.substr(pos_final,longitud);
          }else{
          }
     }




parser.arbol ={ 
    errores: []
};/* generated by jison-lex 0.3.4 */
var lexer = (function(){
var lexer = ({

EOF:1,

parseError:function parseError(str, hash) {
        if (this.yy.parser) {
            this.yy.parser.parseError(str, hash);
        } else {
            throw new Error(str);
        }
    },

// resets the lexer, sets new input
setInput:function (input, yy) {
        this.yy = yy || this.yy || {};
        this._input = input;
        this._more = this._backtrack = this.done = false;
        this.yylineno = this.yyleng = 0;
        this.yytext = this.matched = this.match = '';
        this.conditionStack = ['INITIAL'];
        this.yylloc = {
            first_line: 1,
            first_column: 0,
            last_line: 1,
            last_column: 0
        };
        if (this.options.ranges) {
            this.yylloc.range = [0,0];
        }
        this.offset = 0;
        return this;
    },

// consumes and returns one char from the input
input:function () {
        var ch = this._input[0];
        this.yytext += ch;
        this.yyleng++;
        this.offset++;
        this.match += ch;
        this.matched += ch;
        var lines = ch.match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno++;
            this.yylloc.last_line++;
        } else {
            this.yylloc.last_column++;
        }
        if (this.options.ranges) {
            this.yylloc.range[1]++;
        }

        this._input = this._input.slice(1);
        return ch;
    },

// unshifts one char (or a string) into the input
unput:function (ch) {
        var len = ch.length;
        var lines = ch.split(/(?:\r\n?|\n)/g);

        this._input = ch + this._input;
        this.yytext = this.yytext.substr(0, this.yytext.length - len);
        //this.yyleng -= len;
        this.offset -= len;
        var oldLines = this.match.split(/(?:\r\n?|\n)/g);
        this.match = this.match.substr(0, this.match.length - 1);
        this.matched = this.matched.substr(0, this.matched.length - 1);

        if (lines.length - 1) {
            this.yylineno -= lines.length - 1;
        }
        var r = this.yylloc.range;

        this.yylloc = {
            first_line: this.yylloc.first_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.first_column,
            last_column: lines ?
                (lines.length === oldLines.length ? this.yylloc.first_column : 0)
                 + oldLines[oldLines.length - lines.length].length - lines[0].length :
              this.yylloc.first_column - len
        };

        if (this.options.ranges) {
            this.yylloc.range = [r[0], r[0] + this.yyleng - len];
        }
        this.yyleng = this.yytext.length;
        return this;
    },

// When called from action, caches matched text and appends it on next action
more:function () {
        this._more = true;
        return this;
    },

// When called from action, signals the lexer that this rule fails to match the input, so the next matching rule (regex) should be tested instead.
reject:function () {
        if (this.options.backtrack_lexer) {
            this._backtrack = true;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. You can only invoke reject() in the lexer when the lexer is of the backtracking persuasion (options.backtrack_lexer = true).\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });

        }
        return this;
    },

// retain first n characters of the match
less:function (n) {
        this.unput(this.match.slice(n));
    },

// displays already matched input, i.e. for error messages
pastInput:function () {
        var past = this.matched.substr(0, this.matched.length - this.match.length);
        return (past.length > 20 ? '...':'') + past.substr(-20).replace(/\n/g, "");
    },

// displays upcoming input, i.e. for error messages
upcomingInput:function () {
        var next = this.match;
        if (next.length < 20) {
            next += this._input.substr(0, 20-next.length);
        }
        return (next.substr(0,20) + (next.length > 20 ? '...' : '')).replace(/\n/g, "");
    },

// displays the character position where the lexing error occurred, i.e. for error messages
showPosition:function () {
        var pre = this.pastInput();
        var c = new Array(pre.length + 1).join("-");
        return pre + this.upcomingInput() + "\n" + c + "^";
    },

// test the lexed token: return FALSE when not a match, otherwise return token
test_match:function(match, indexed_rule) {
        var token,
            lines,
            backup;

        if (this.options.backtrack_lexer) {
            // save context
            backup = {
                yylineno: this.yylineno,
                yylloc: {
                    first_line: this.yylloc.first_line,
                    last_line: this.last_line,
                    first_column: this.yylloc.first_column,
                    last_column: this.yylloc.last_column
                },
                yytext: this.yytext,
                match: this.match,
                matches: this.matches,
                matched: this.matched,
                yyleng: this.yyleng,
                offset: this.offset,
                _more: this._more,
                _input: this._input,
                yy: this.yy,
                conditionStack: this.conditionStack.slice(0),
                done: this.done
            };
            if (this.options.ranges) {
                backup.yylloc.range = this.yylloc.range.slice(0);
            }
        }

        lines = match[0].match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno += lines.length;
        }
        this.yylloc = {
            first_line: this.yylloc.last_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.last_column,
            last_column: lines ?
                         lines[lines.length - 1].length - lines[lines.length - 1].match(/\r?\n?/)[0].length :
                         this.yylloc.last_column + match[0].length
        };
        this.yytext += match[0];
        this.match += match[0];
        this.matches = match;
        this.yyleng = this.yytext.length;
        if (this.options.ranges) {
            this.yylloc.range = [this.offset, this.offset += this.yyleng];
        }
        this._more = false;
        this._backtrack = false;
        this._input = this._input.slice(match[0].length);
        this.matched += match[0];
        token = this.performAction.call(this, this.yy, this, indexed_rule, this.conditionStack[this.conditionStack.length - 1]);
        if (this.done && this._input) {
            this.done = false;
        }
        if (token) {
            return token;
        } else if (this._backtrack) {
            // recover context
            for (var k in backup) {
                this[k] = backup[k];
            }
            return false; // rule action called reject() implying the next rule should be tested instead.
        }
        return false;
    },

// return next match in input
next:function () {
        if (this.done) {
            return this.EOF;
        }
        if (!this._input) {
            this.done = true;
        }

        var token,
            match,
            tempMatch,
            index;
        if (!this._more) {
            this.yytext = '';
            this.match = '';
        }
        var rules = this._currentRules();
        for (var i = 0; i < rules.length; i++) {
            tempMatch = this._input.match(this.rules[rules[i]]);
            if (tempMatch && (!match || tempMatch[0].length > match[0].length)) {
                match = tempMatch;
                index = i;
                if (this.options.backtrack_lexer) {
                    token = this.test_match(tempMatch, rules[i]);
                    if (token !== false) {
                        return token;
                    } else if (this._backtrack) {
                        match = false;
                        continue; // rule action called reject() implying a rule MISmatch.
                    } else {
                        // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
                        return false;
                    }
                } else if (!this.options.flex) {
                    break;
                }
            }
        }
        if (match) {
            token = this.test_match(match, rules[index]);
            if (token !== false) {
                return token;
            }
            // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
            return false;
        }
        if (this._input === "") {
            return this.EOF;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. Unrecognized text.\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });
        }
    },

// return next match that has a token
lex:function lex () {
        var r = this.next();
        if (r) {
            return r;
        } else {
            return this.lex();
        }
    },

// activates a new lexer condition state (pushes the new lexer condition state onto the condition stack)
begin:function begin (condition) {
        this.conditionStack.push(condition);
    },

// pop the previously active lexer condition state off the condition stack
popState:function popState () {
        var n = this.conditionStack.length - 1;
        if (n > 0) {
            return this.conditionStack.pop();
        } else {
            return this.conditionStack[0];
        }
    },

// produce the lexer rule set which is active for the currently active lexer condition state
_currentRules:function _currentRules () {
        if (this.conditionStack.length && this.conditionStack[this.conditionStack.length - 1]) {
            return this.conditions[this.conditionStack[this.conditionStack.length - 1]].rules;
        } else {
            return this.conditions["INITIAL"].rules;
        }
    },

// return the currently active lexer condition state; when an index argument is provided it produces the N-th previous condition state, if available
topState:function topState (n) {
        n = this.conditionStack.length - 1 - Math.abs(n || 0);
        if (n >= 0) {
            return this.conditionStack[n];
        } else {
            return "INITIAL";
        }
    },

// alias for begin(condition)
pushState:function pushState (condition) {
        this.begin(condition);
    },

// return the number of states currently on the stack
stateStackSize:function stateStackSize() {
        return this.conditionStack.length;
    },
options: {"case-insensitive":true},
performAction: function anonymous(yy,yy_,$avoiding_name_collisions,YY_START) {
var YYSTATE=YY_START;
switch($avoiding_name_collisions) {
case 0:
break;
case 1:
break;
case 2: yy_.yytext = get_type_xml(yy_.yytext,yy_.yylloc.first_line,yy_.yylloc.first_column); return 7; 
break;
case 3: let name=tag_principal(yy_.yytext); yy_.yytext= return_only_id(yy_.yytext); this.begin("Etiqueta"); return name; 
break;
case 4:
break;
case 5:
break;
case 6: return 22; 
break;
case 7: return 23; 
break;
case 8: yy_.yytext=change_especials(yy_.yytext); return 24; 
break;
case 9: this.popState(); return tag_principal(yy_.yytext); 
break;
case 10:  
break;
case 11:return 28
break;
case 12:return 29
break;
case 13:return 30
break;
case 14:return 31
break;
case 15:return 32
break;
case 16:return 27
break;
case 17:return 6
break;
case 18: parser.arbol.errores.push({tipo : 'Lexico', mensaje: yy_.yytext , linea: yy_.yylloc.first_line , columna: yy_.yylloc.first_column}); 
break;
}
},
rules: [/^(?:[\s\r\t\n]+)/i,/^(?:[<][!][-][-][^-]*[-]+([^<!--][^-]*[-]+)*[>])/i,/^(?:[<][?]([^<?]*)*[?][>])/i,/^(?:[<][("\/")]?[A-Za-z_\ñ\Ñ][A-Za-z_0-9\ñ\Ñ]*)/i,/^(?:[ \r\t]+)/i,/^(?:\n)/i,/^(?:[A-Za-z_\ñ\Ñ][A-Za-z_0-9\ñ\Ñ]*)/i,/^(?:=)/i,/^(?:([\"\“\”](([^\"\“\”\\])*([\\].)*)*[\"\“\”])|([\'](([^\'\\])*([\\].)*)*[\']))/i,/^(?:[\/]?[>])/i,/^(?:.)/i,/^(?:&lt;)/i,/^(?:&gt;)/i,/^(?:&amp;)/i,/^(?:&apos;)/i,/^(?:&quot;)/i,/^(?:([^><&'"])+)/i,/^(?:$)/i,/^(?:.)/i],
conditions: {"Etiqueta":{"rules":[0,4,5,6,7,8,9,10],"inclusive":false},"INITIAL":{"rules":[0,1,2,3,11,12,13,14,15,16,17,18],"inclusive":true}}
});
return lexer;
})();
parser.lexer = lexer;
function Parser () {
  this.yy = {};
}
Parser.prototype = parser;parser.Parser = Parser;
return new Parser;
})();


if (typeof require !== 'undefined' && typeof exports !== 'undefined') {
exports.parser = gramar_des_cy;
exports.Parser = gramar_des_cy.Parser;
exports.parse = function () { return gramar_des_cy.parse.apply(gramar_des_cy, arguments); };
exports.main = function commonjsMain (args) {
    if (!args[1]) {
        console.log('Usage: '+args[0]+' FILE');
        process.exit(1);
    }
    var source = require('fs').readFileSync(require('path').normalize(args[1]), "utf8");
    return exports.parser.parse(source);
};
if (typeof module !== 'undefined' && require.main === module) {
  exports.main(process.argv.slice(1));
}
}